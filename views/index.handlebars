<h1>Burgers!</h1>

<h2></h2>

<ul>
  {{#each }}
    {{#unless }}
      {{> }}
    {{/unless}}
  {{/each}}
</ul>

<h2></h2>

<ul>
  {{#each }}
    {{#if }}
      {{> }}
    {{/if}}
  {{/each}}
</ul>

<h2></h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Hungry?</label><br>
    <input type="radio" name="burger1" value="1" checked> Sleepy!<br>
    <input type="radio" name="burger2" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>
